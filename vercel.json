{
  "buildCommand": "pnpm run build",
  "installCommand": "corepack enable && corepack prepare pnpm@9.0.0 --activate && export PATH=\"$(corepack pnpm bin):$PATH\" && pnpm install",
  "framework": null
}
